# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*.backup

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, by default
*.tfvars
*.tfvars.json

# Exclude all .auto.tfvars files, which are likely to contain sensitive data, by default
*.auto.tfvars
*.auto.tfvars.json

# Ignore override files as they are usually used for local customizations
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*
tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc
